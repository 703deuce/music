# Music AI API Suite - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore

# =============================================================================
# RUNPOD CONFIGURATION (Required)
# =============================================================================
RUNPOD_AI_API_KEY=runpod-your-api-key-here
RUNPOD_ENDPOINT_URL=https://your-endpoint-id.runpod.io
RUNPOD_VOLUME_PATH=/runpod-volume

# =============================================================================
# CLOUD STORAGE (Choose one - required for audio file storage)
# =============================================================================

# Option A: Firebase Storage (Your Current Setup)
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id
FIREBASE_MEASUREMENT_ID=your-measurement-id

# Option B: AWS S3 (Alternative)
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=your-secret-key
# S3_BUCKET=your-audio-bucket
# AWS_REGION=us-east-1

# Option C: Google Cloud Storage (Alternative)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GCS_BUCKET=your-audio-bucket

# Option D: Azure Blob Storage (Alternative)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;...
# AZURE_CONTAINER=your-audio-container

# =============================================================================
# MODEL DOWNLOAD TOKENS (Recommended)
# =============================================================================

# HuggingFace Token (for faster downloads and private models)
HUGGINGFACE_HUB_TOKEN=hf_your_token_here

# =============================================================================
# CONTAINER REGISTRY (Required for deployment)
# =============================================================================

# Docker Hub (Easiest option)
DOCKER_USERNAME=your-docker-username
DOCKER_PASSWORD=your-docker-password

# Or GitHub Container Registry
# GITHUB_TOKEN=ghp_your_github_token

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================

# OpenAI (for prompt enhancement)
OPENAI_API_KEY=sk-your-openai-key

# Replicate (alternative model hosting)
REPLICATE_API_TOKEN=r8_your-replicate-token

# =============================================================================
# MODEL PATHS (Auto-configured, don't change unless needed)
# =============================================================================
ACE_STEP_MODEL_PATH=/runpod-volume/models/ace-step
SOVITS_MODELS_DIR=/runpod-volume/models/sovits
DEMUCS_MODELS_DIR=/runpod-volume/models/demucs
PYTHONPATH=/workspace
