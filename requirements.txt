# RunPod Music AI API Suite - Core Dependencies Only
# Each AI model will handle its own dependencies when installed

# Core Python dependencies for our handler
torch>=2.0.0
torchaudio>=2.0.0
numpy==1.24.3
soundfile>=0.12.1
requests>=2.28.0

# Essential audio processing
librosa>=0.9.0
pydub>=0.25.1

# Machine Learning basics
transformers>=4.21.0
huggingface_hub>=0.16.0

# Only install models that are available on PyPI
demucs>=4.0.0
matchering>=2.0.5

# Utilities for our handler
python-dotenv>=1.0.0
pydantic>=2.0.0
psutil>=5.9.0

# RunPod integration
runpod>=1.0.0
